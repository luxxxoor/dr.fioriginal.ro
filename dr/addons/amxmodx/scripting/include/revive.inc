#pragma reqlib "revive"

/**
 * Reinvie un jucator prin intermediul pluginului de revive, folosindu-i proprietatiile.
 *
 * @Param : @Index - Indexul jucatorului.
 *
 * @Return : Nu are valoare de return
 */
native revivePlayer(Index);